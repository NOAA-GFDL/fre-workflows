name: run_ppp
on:
  pull_request:
    branches:
      - main
# cancel running jobs if theres a newer push
#concurrency:
#  group: ${{ github.workflow }}-${{ github.ref }}
#  cancel-in-progress: true

jobs:
  test-cloud:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run ppp-container
        run: |
          # Print pwd then run script
          echo "Currently in:"
          cwd=$(pwd)
          echo $cwd

          gridspec_file=c96_OM4_025_grid_No_mg_drag_v20160808.tar
          bind_paths="--bind /contrib/container-test/ppp-setup/:/mnt:rw
                      --bind /contrib/container-test/history:/mnt/history:ro
                      --bind /contrib/container-test/grid_spec/${gridspec_file}:/mnt/${gridspec_file}:ro
                      --bind /contrib/container-test"

          container_path=/contrib/container-test/ppp_8-22.sif
          runscript_path=${cwd}/for_gh_runner/runscript.sh

          singularity exec --writable-tmpfs ${bind_paths} ${container_path} ${runscript_path}

          # Can potentially do this to get list of tasks
          #task_list=$(cylc list test_pp__ptest__ttest)
          #task1=$(echo $task_list | cut -d ' ' -f1) ....


      - name: Upload workflow-run log files
        uses: actions/upload-artifact@v4
        with:
          name: fre-workflow-logs
          path: /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log
          if-no-files-found: error

      - name: Print pp-starter successes or failures
        run: |
          # Successes
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/pp-starter/0[0-3]/job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/pp-starter/0[0-3]/job.status)
          exit_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print stage-history successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/stage-history/0[0-3]/job.status)
          exist_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/stage-history/0[0-3]/job.status)
          exist_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi
      - name: Print regrid-xy successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/regrid-xy*/0[0-3]/job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/regrid-xy*/0[0-3]/job.status)
          exit_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print remap-pp-components successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/remap-pp-components*/0[0-3]/job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/remap-pp-components*/0[0-3]/job.status)
          exit_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print make-timeavgs successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/make-timeavgs*/0[0-3]/job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/make-timeavgs*/0[0-3]/job.status)
          exit_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print rename-split-to-pp successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/rename-split-to-pp*/0[0-3]/job.status)
          exit_status_2=$? 

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/rename-split-to-pp*/0[0-3]/job.status)
          exit_status=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print split-netcdf successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/split-netcdf*/0[0-3]/job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/split-netcdf*/0[0-3]/job.status)
          exit_status_2=$? 

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print combine-timeavgs successes or failures
        run: |
          # Successes 
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/combine-timeavgs*/0[0-3]/job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/combine-timeavgs*/0[0-3]/job.status)
          exit_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

      - name: Print clean task successes or failures
        run: |
          # Successes
          success=$(grep -E "CYLC_JOB_EXIT=SUCCEEDED" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/clean*/0./job.status)
          exit_status_1=$?

          # Failures
          failure=$(grep -E "CYLC_JOB_EXIT=ERR" /contrib/container-test/ppp-setup/cylc-run/test_pp__ptest__ttest/log/job/*/clean*/0[0-3]/job.status)
          exit_status_2=$?

          if [ $exist_status_1 -eq 0 ]; then
              echo $success
          fi

          if [ exit_status_2 -eq 0 ];
              echo "Failures found"
              echo $failure
          fi

## Keep in here for now as a just in case reference ##
#      - name: Print any succeeded tasks
#        run: |
#          # List tasks that have succeeded (and completed) in the workflow
#          grep -E ":succeeded.*completed" /contrib/container-test/ppp-setup/log.out
#
#      - name: Print any failed tasks
#        run: |
#          # Search for if there is an ERR status for any task
#          if grep -q "failed/ERR" /contrib/container-test/ppp-setup/log.out; then
#              echo "Task failures found: CHECK THE LOG FILES UPLOADED AS ARTIFACTS"
#              grep "failed/ERR" /contrib/container-test/ppp-setup/log.out
#              exit 1
#          fi
#
#          echo "Workflow ran successfully"

##To-do:
# - figure out if container will be hosted on cloud or somehow pulled from registry
# - examine behavior of runner in draft vs not draft mode
