name: create_test_conda_env

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '>=3.9'

      - name: Add conda to system path
        run: |
          # $CONDA is an env var pointing to root of miniconda dir
          echo $CONDA/bin >> $GITHUB_PATH

      - name: Create fre-workflows environment
        run: |
          # create environment containing all dependencies
          # the env cannot be explicitly activated in github CI/CD
          conda env create -f environment.yml --name fre-workflows

          # add conda env's executables to github's PATH equiv.
          echo $CONDA/envs/fre-workflows/bin >> $GITHUB_PATH

      - name: in fre-workflows environment, cylc lint
        continue-on-error: true
        run: |
          # lint .cylc workflow files
          cylc lint -v

      - name: data_lineage unittest
        continue-on-error: true
        run: |
          python -m unittest discover -s data_lineage/test -v

      - name: data_lineage pylint
        continue-on-error: true
        run: |
          pylint -v data_lineage

      - name: make-timeseries pytest
        continue-on-error: true
        run: |
          pytest -v -v -rx --cov=app/make-timeseties/bin/make-timeseries ./app/make-timeseries/test

      - name: make-timeseries pylint
        continue-on-error: true
        run: |
          pylint -v app/make-timeseties/bin/make-timeseries

      - name: remap-pp-components pytest
        continue-on-error: true
        run: |
          pytest -v -v -rx --cov=app/remap-pp-components/bin/remap-pp-components ./app/remap-pp-components/test

      - name: remap-pp-components pylint
        continue-on-error: true
        run: |
          pylint -v app/remap-pp-components/bin/remap-pp-components

      - name: PPANHandler pytest
        continue-on-error: true
        run: |
          pytest -v -v -rx ./tests/test_PPANHandler.py

      - name: PPANHandler pylint
        continue-on-error: true
        run: |
          pylint ./lib/python/PPANHandler.py

      - name: papiex_tooler pytest
        continue-on-error: true
        run: |
          pytest -v -v -rx ./tests/test_papiex_tooler.py

      - name: papiex_ops and tool_ops_w_papiex pylint
        continue-on-error: true
        run: |
          pylint ./lib/python/tool_ops_w_papiex.py
          pylint ./lib/python/papiex_ops.py

      - name: Jinja2filter pytest
        continue-on-error: false
        run: |
          pytest -v ./Jinja2Filters/tests

      - name: Jinja2filter pylint
        continue-on-error: true
        run: |
          pylint ./Jinja2Filters

      - name: chunkchecker pytest
        continue-on-error: true
        run: |
          pytest -v -v -rx ./tests/test_chunkcheck.py

      - name: python macros pylint
        continue-on-error: true
        run: |
          pylint ./meta/lib/python/macros/
